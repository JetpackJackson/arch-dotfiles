#!/bin/env bash
#typeset -a os
#for o in `expac -S '%o\n' $pkgs`
#do
#  read -p "Install ${o}? " r
#  [[ ${r,,} =~ ^y(|e|es)$ ]] && os+=( $o )
#done
#expac -S "%n %o" $pkg | pacman -S -
#sudo pacman -S $pkgs ${os[@]}
